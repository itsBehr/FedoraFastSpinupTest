Run:
```curl -L "https://github.com/itsBehr/FedoraFastSpinupTest/raw/refs/heads/maincandidate-v2/init.sh" | bash``` 
and bash the BehrFFS.sh located on your desktop after each restart. There will be 2-3 depending on settings chosen at startup. 
